# ==============================================================================
# G-SCORES ENVIRONMENT CONFIGURATION
# ==============================================================================

# Application Port & Node Environment
PORT=3000
NODE_ENV=development

# ------------------------------------------------------------------------------
# MODE 1 (DEFAULT): Supabase Cloud Database
# Ready out-of-the-box for evaluators without any setup.
# ------------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres.pszuzryedizlqylhejon:vuquang123@@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.pszuzryedizlqylhejon:vuquang123@@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"

# ------------------------------------------------------------------------------
# MODE 2: Local Docker PostgreSQL (Uncomment below if using docker-compose.dev.yml)
# Command: docker compose -f docker-compose.dev.yml up -d
# ------------------------------------------------------------------------------
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gscores?schema=public"
# DIRECT_URL="postgresql://postgres:postgres@localhost:5432/gscores?schema=public"

# ------------------------------------------------------------------------------
# MODE 3: Full Docker Compose Production (Overridden automatically in docker-compose.yml)
# Command: docker compose up -d --build
# ------------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gscores

# Web Frontend Config
VITE_API_BASE_URL=http://localhost:3000
